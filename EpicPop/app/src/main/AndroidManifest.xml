<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permisos necesarios para EpicPop -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Features de hardware -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.fingerprint" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Epic_pop"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- SplashActivity como launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Epic_pop.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividades de autenticación -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.Epic_pop.NoActionBar" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.Epic_pop.NoActionBar" />

        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false"
            android:theme="@style/Theme.Epic_pop.NoActionBar" />

        <!-- MainActivity principal -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Epic_pop.NoActionBar" />

        <!-- Actividades de retos -->
        <activity
            android:name=".AddChallengeActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".EditChallengeActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ChallengeListActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Actividades de gamificación -->
        <activity
            android:name=".AchievementsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".CalendarActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Actividades de usuario -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".SettingsOptionActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <!-- Nueva actividad de FAQ -->
        <activity
            android:name=".FAQActivity"
            android:exported="false"
            android:parentActivityName=".FeedbackActivity" />

        <!-- Nueva actividad de cambiar contraseña -->
        <activity
            android:name=".ChangePasswordActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <!-- Actividades específicas de configuración -->
        <activity
            android:name=".NotificationSettingsActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".ThemeSettingsActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".FeedbackActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <!-- Actividades de IA y música -->
        <activity
            android:name=".AIAssistantActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".MusicPlayerActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- FileProvider para cámara -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".PrivacySecurityActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".EditProfileActivity"
            android:exported="false"
            android:parentActivityName=".ProfileActivity" />

    </application>

</manifest>